# viz 功能域环境|viz domain environment
# 
# 创建|Create: mamba env create -f envs/viz.yml
# 由求解预检验证(2026-08-16, 全部可解)|Validated by solver preflight
name: viz
channels:
  - conda-forge
  - bioconda
dependencies:
  - samplot
  - pycirclize
