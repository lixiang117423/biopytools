# r 功能域环境|r domain environment
# 
# 创建|Create: mamba env create -f envs/r.yml
# 由求解预检验证(2026-08-16, 全部可解)|Validated by solver preflight
name: r
channels:
  - conda-forge
  - bioconda
dependencies:
  - r-qtl
  - r-wgcna
